[flake8]
max-line-length = 120
# E203 (whitespace before ':') conflicts with black's slice formatting.
extend-ignore = E203
extend-exclude =
    regexsolver/_generated,
    .venv,
    venv,
    build,
    dist
